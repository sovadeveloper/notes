spring:
  datasource:
    url: jdbc:h2:mem:notes
    driverClassName: org.h2.Driver
    username: admin
    password: root
  jpa:
    database-platform: org.hibernate.dialect.H2Dialect
    show-sql: true
    hibernate:
      ddl-auto: none
  h2:
    console:
      enabled: true
